# Form Actions — Learned and Demonstrated

Form actions (`+page.server.ts` `actions` export) were entirely new. The user completed all lesson quizzes and built working hands-on exercises in the project. This is not just exposure — the pattern is now operational.

**Evidence**: All interactive exercises fully working in code.

**Implications**: Can now use named and default actions, `fail()`, `superforms`-style validation patterns, and progressive enhancement via `use:enhance`. Do not re-teach the basics of this pattern. The next session can build on form actions as a known primitive — e.g., combining them with `hooks.server.ts` guards.
